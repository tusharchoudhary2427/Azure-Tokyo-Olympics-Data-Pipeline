##### **Secrets like Storage Account, Container name, Client IDs, and Tenant IDs, Secret IDs, must never be pushed to GitHub. That’s why I removed them from my public code. I use environment variables or linked services in Azure to store and access them securely.**

##### 

##### **If you need to see how I use those credentials, I can show you the code structure with placeholder values, but I cannot share the actual secrets for security reasons.**

